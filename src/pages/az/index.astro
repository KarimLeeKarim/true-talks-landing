---
import Layout from '../../layouts/Layout.astro';
import LandingPage from '../../components/LandingPage';
import { translations } from '../../i18n/translations';

const { meta } = translations.az;
---

<Layout lang="az" title={meta.title} description={meta.description} keywords={meta.keywords}>
  <LandingPage lang="az" client:visible />
</Layout>
